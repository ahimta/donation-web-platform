{
  "rules": {
    "users": {
      ".read": true,
      "$uid": {
        ".write": "$uid === auth.uid"
      }
    },
    "foodDonations": {
      ".read": true,
      "$id": {
        ".write": "newData.child('donorId').val() === auth.uid"
      }
    },
    "otherDonations": {
      ".read": true,
      "$id": {
        ".write": "newData.child('donorId').val() === auth.uid"
      }
    }
  }
}
